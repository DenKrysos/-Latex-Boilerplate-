{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
          "label": "latexmk_plain",
          "type": "shell",
          "command": "${config:latex-workshop.latex.tools[0].command}",
          "args": [
            "${config:latex-workshop.latex.tools[0].args}"
          ],
          "group": {
            "kind": "build",
            "isDefault": true
          }
        }
    ]
}